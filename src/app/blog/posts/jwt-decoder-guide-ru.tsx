import { ToolCTA } from "@/components/blog/ToolCTA";

export default function Content() {
  return (
    <div>
      <p>
        Если вы работали с любой современной системой веб-аутентификации — OAuth 2.0, OpenID Connect или
        собственным API — вы почти наверняка сталкивались с JWT-токенами. Они появляются в заголовках Authorization,
        в cookie, в локальном хранилище и в сессиях отладки в 2 часа ночи, когда поток входа загадочно сбоит.
        Понимание того, что на самом деле содержит JWT, как его читать и как замечать частые проблемы, делает отладку
        аутентификации значительно быстрее.
      </p>
      <ToolCTA slug="jwt-decoder" variant="inline" />
      <p>
        <a href="/tools/jwt-decoder">Декодировщик JWT BrowseryTools</a> позволяет вставить любой JWT-токен и
        мгновенно увидеть его декодированный заголовок, полезную нагрузку и статус истечения — всё в браузере, причём
        токен никогда не покидает ваше устройство.
      </p>

      <h2>Что такое JWT?</h2>
      <p>
        JWT расшифровывается как JSON Web Token и определён в RFC 7519. JWT — это компактный, URL-безопасный токен,
        кодирующий набор утверждений (claims) — заявлений о субъекте, обычно пользователе — в формате, который можно
        проверить и которому можно доверять. Ключевое свойство JWT в том, что он <em>самодостаточен</em>: сам токен
        несёт всю информацию, нужную серверу для аутентификации запроса, без обращения к базе данных.
      </p>
      <p>
        JWT выглядит так:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.8rem", lineHeight: 1.7, wordBreak: "break-all" as const}}>
{`eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c`}
      </pre>
      <p>
        На первый взгляд он выглядит как абракадабра. Но у него очень точная структура: три раздела в кодировке
        Base64URL, разделённые точками. У каждого раздела своё назначение.
      </p>

      <h2>Трёхчастная структура: Header.Payload.Signature</h2>

      <h3>Часть 1: заголовок</h3>
      <p>
        Первый сегмент, до первой точки — это <strong>заголовок</strong> (header). Это объект JSON в кодировке
        Base64URL, описывающий тип токена и алгоритм подписи. В декодированном виде заголовок из примера выше
        выглядит так:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`{
  "alg": "HS256",
  "typ": "JWT"
}`}
      </pre>
      <p>
        Поле <code>alg</code> задаёт алгоритм подписи. Распространённые значения, с которыми вы столкнётесь:
      </p>
      <ul>
        <li>
          <strong>HS256</strong> — HMAC с SHA-256. Использует общий секретный ключ. И эмитент, и проверяющий должны
          иметь один и тот же секрет. Распространён в монолитных приложениях.
        </li>
        <li>
          <strong>RS256</strong> — RSA-подпись с SHA-256. Использует пару открытый/закрытый ключ. Эмитент подписывает
          закрытым ключом; проверяющие проверяют открытым. Распространён в распределённых системах и у
          OAuth-провайдеров.
        </li>
        <li>
          <strong>ES256</strong> — ECDSA с P-256 и SHA-256. Как RS256, но с использованием эллиптических кривых —
          более короткие ключи, тот же уровень безопасности. Предпочтителен в современных высокопроизводительных
          системах.
        </li>
        <li>
          <strong>none</strong> — без подписи. Никогда не принимайте это в продакшене. Печально известная уязвимость
          возникает, когда серверы принимают неподписанные токены, потому что клиент изменил <code>alg</code>{" "}
          на <code>"none"</code>.
        </li>
      </ul>

      <h3>Часть 2: полезная нагрузка</h3>
      <p>
        Второй сегмент — это <strong>полезная нагрузка</strong> (payload) — собственно данные, которые несёт токен.
        Это тоже объект JSON в кодировке Base64URL. Декодированная полезная нагрузка из нашего примера:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`{
  "sub": "usr_84219",
  "name": "Jane Doe",
  "email": "jane.doe@example.com",
  "roles": ["user", "editor"],
  "iss": "https://auth.example.com",
  "aud": "https://api.example.com",
  "iat": 1738368000,
  "exp": 1738371600,
  "jti": "7f3a9b4c-1d2e-456f-abcd-890123456789"
}`}
      </pre>
      <p>
        Полезная нагрузка содержит два типа утверждений: <strong>зарегистрированные утверждения</strong>,
        определённые спецификацией JWT, и <strong>частные/пользовательские утверждения</strong>, добавленные вашим
        приложением (вроде
        <code>name</code>, <code>email</code> и <code>roles</code> выше).
      </p>

      <h3>Часть 3: подпись</h3>
      <p>
        Третий сегмент — это <strong>подпись</strong> (signature). Она вычисляется, беря заголовок в кодировке
        Base64URL, точку, полезную нагрузку в кодировке Base64URL и подписывая результат алгоритмом и ключом,
        указанными в заголовке. Для HS256:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.85rem", lineHeight: 1.7}}>
{`HMAC-SHA256(
  base64url(header) + "." + base64url(payload),
  secret
)`}
      </pre>
      <p>
        Подпись обеспечивает целостность: если кто-то изменит хотя бы один символ в заголовке или полезной нагрузке
        после выдачи токена, подпись становится недействительной и проверка не проходит. Не зная секрет подписи (или
        закрытый ключ эмитента для RS256/ES256), злоумышленник не может подделать действительный токен.
      </p>

      <h2>Справочник стандартных утверждений JWT</h2>
      <div style={{overflowX: "auto", margin: "1.5rem 0"}}>
        <table style={{width: "100%", borderCollapse: "collapse", fontSize: "0.9rem"}}>
          <thead>
            <tr style={{background: "rgba(99,102,241,0.15)"}}>
              <th style={{padding: "10px 16px", textAlign: "left", borderBottom: "2px solid rgba(99,102,241,0.3)"}}>Утверждение</th>
              <th style={{padding: "10px 16px", textAlign: "left", borderBottom: "2px solid rgba(99,102,241,0.3)"}}>Полное название</th>
              <th style={{padding: "10px 16px", textAlign: "left", borderBottom: "2px solid rgba(99,102,241,0.3)"}}>Описание</th>
            </tr>
          </thead>
          <tbody>
            <tr style={{borderBottom: "1px solid rgba(0,0,0,0.08)"}}>
              <td style={{padding: "10px 16px"}}><code>iss</code></td>
              <td style={{padding: "10px 16px"}}>Issuer (эмитент)</td>
              <td style={{padding: "10px 16px"}}>Кто выдал токен (например, URL вашего сервера авторизации)</td>
            </tr>
            <tr style={{borderBottom: "1px solid rgba(0,0,0,0.08)", background: "rgba(0,0,0,0.02)"}}>
              <td style={{padding: "10px 16px"}}><code>sub</code></td>
              <td style={{padding: "10px 16px"}}>Subject (субъект)</td>
              <td style={{padding: "10px 16px"}}>О ком токен — обычно уникальный ID пользователя</td>
            </tr>
            <tr style={{borderBottom: "1px solid rgba(0,0,0,0.08)"}}>
              <td style={{padding: "10px 16px"}}><code>aud</code></td>
              <td style={{padding: "10px 16px"}}>Audience (аудитория)</td>
              <td style={{padding: "10px 16px"}}>Для каких сервисов предназначен токен</td>
            </tr>
            <tr style={{borderBottom: "1px solid rgba(0,0,0,0.08)", background: "rgba(0,0,0,0.02)"}}>
              <td style={{padding: "10px 16px"}}><code>exp</code></td>
              <td style={{padding: "10px 16px"}}>Expiration Time (срок истечения)</td>
              <td style={{padding: "10px 16px"}}>Unix-метка времени, после которой токен должен быть отвергнут</td>
            </tr>
            <tr style={{borderBottom: "1px solid rgba(0,0,0,0.08)"}}>
              <td style={{padding: "10px 16px"}}><code>iat</code></td>
              <td style={{padding: "10px 16px"}}>Issued At (выдан в)</td>
              <td style={{padding: "10px 16px"}}>Unix-метка времени создания токена</td>
            </tr>
            <tr style={{borderBottom: "1px solid rgba(0,0,0,0.08)", background: "rgba(0,0,0,0.02)"}}>
              <td style={{padding: "10px 16px"}}><code>nbf</code></td>
              <td style={{padding: "10px 16px"}}>Not Before (не ранее)</td>
              <td style={{padding: "10px 16px"}}>Токен недействителен до этой Unix-метки времени</td>
            </tr>
            <tr>
              <td style={{padding: "10px 16px"}}><code>jti</code></td>
              <td style={{padding: "10px 16px"}}>JWT ID</td>
              <td style={{padding: "10px 16px"}}>Уникальный идентификатор токена — используется для предотвращения атак повторного воспроизведения</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h2>Почему утверждение exp критически важно</h2>
      <p>
        Утверждение <code>exp</code> — это Unix-метка времени, число секунд с 1 января 1970 года (UTC). В нашем
        примере <code>"exp": 1738371600</code>. Чтобы преобразовать это в читаемую человеком дату, можно
        использовать JavaScript:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`new Date(1738371600 * 1000).toUTCString()
// → "Sat, 01 Feb 2026 01:00:00 GMT"`}
      </pre>
      <p>
        Истечение JWT — первое, что стоит проверить, когда токен отвергается. Токен, действительный вчера, не пройдёт
        сегодня, если его <code>exp</code> в прошлом — так задумано. Короткоживущие токены (от 15 минут до 1 часа)
        ограничивают окно ущерба при краже токена. Долгоживущие токены (дни или недели) удобнее, но опаснее при
        компрометации.
      </p>
      <p>
        <a href="/tools/jwt-decoder">Декодировщик JWT BrowseryTools</a> автоматически считывает утверждения{" "}
        <code>exp</code> и <code>iat</code> и отображает их как читаемые человеком даты рядом с сырыми Unix-метками
        времени, так что вам никогда не придётся считать в уме.
      </p>

      <h2>Распространённые сценарии отладки JWT</h2>

      <h3>Токен истёк (401 Unauthorized)</h3>
      <p>
        Самая частая ошибка JWT. Сервер отверг токен, потому что текущее время позже значения
        <code>exp</code>. Решение: реализуйте поток обновления токена с помощью более долгоживущего refresh-токена
        или просто переаутентифицируйтесь. Вставьте токен в декодировщик, чтобы точно подтвердить, когда он истёк.
      </p>

      <h3>Неправильная аудитория</h3>
      <p>
        Если ваш API проверяет утверждение <code>aud</code>, а токен был выдан для другой аудитории (например, токен,
        выданный для <code>https://api-staging.example.com</code>, отправляется на{" "}
        <code>https://api.example.com</code>), сервер его отвергнет. Декодируйте токен и проверьте поле
        <code>aud</code>, чтобы убедиться, что оно совпадает с тем, что ожидает принимающий сервис.
      </p>

      <h3>Несовпадение алгоритма</h3>
      <p>
        Если ваш сервер ожидает RS256, но получает токен, подписанный HS256 (или наоборот), проверка не проходит.
        Это может случиться при ротации ключей или при смене auth-провайдеров. Сверьте поле <code>alg</code>{" "}
        в декодированном заголовке с тем, что ваш сервер настроен принимать.
      </p>

      <h3>Подпись недействительна</h3>
      <p>
        Если полезную нагрузку подделали — даже один символ изменили — подпись не совпадёт. Это также случается, если
        вы используете неверный секрет или неверный открытый ключ для проверки. Декодирование заголовка и полезной
        нагрузки (которое не требует секрета) позволяет хотя бы рассмотреть, что заявляет токен, даже если вы не
        можете проверить его подлинность на стороне клиента.
      </p>

      <div style={{background: "#fef3c7", borderLeft: "4px solid #f59e0b", padding: "1rem 1.25rem", borderRadius: "0.5rem", margin: "1.5rem 0"}}>
        <strong>Предупреждение о безопасности — полезная нагрузка не зашифрована:</strong> полезная нагрузка JWT
        закодирована в Base64URL, а не зашифрована. Любой, у кого есть строка токена, может декодировать заголовок и
        полезную нагрузку без какого-либо ключа или секрета. Никогда не храните чувствительную информацию в полезной
        нагрузке JWT — ни паролей, ни данных платёжных карт, ни номеров социального страхования, ни закрытых ключей.
        Относитесь к полезной нагрузке как к публично читаемым данным, которые лишь защищены от подделки, а не
        конфиденциальны.
      </div>

      <h2>JWT против сессионных токенов: когда какой использовать</h2>
      <p>
        JWT и традиционные сессионные токены решают одну задачу — идентификацию аутентифицированного пользователя в
        нескольких запросах — но делают это по-разному, и ни один не лучше повсеместно.
      </p>
      <p>
        <strong>Традиционные сессионные токены</strong> — это непрозрачные случайные строки (например, UUID),
        хранимые на стороне сервера в хранилище сессий (Redis, база данных). На каждом запросе сервер ищет токен в
        хранилище и извлекает данные пользователя. У сервера полный контроль: аннулирование сессии немедленно
        отзывает доступ.
      </p>
      <p>
        <strong>JWT</strong> не имеют состояния. Сервер выдаёт подписанный токен и не хранит запись о нём. На каждом
        запросе сервер проверяет подпись и доверяет утверждениям без обращения к базе данных. Это масштабируется
        горизонтально без общего состояния — любой сервер с ключом проверки может аутентифицировать запрос. Размен:
        вы не можете немедленно отозвать JWT до его истечения (если только не реализуете блок-лист токенов, что снова
        вводит состояние).
      </p>
      <ul>
        <li>Используйте <strong>JWT</strong> для микросервисов без состояния, распределённых систем, мобильных API
        и кросс-доменной аутентификации (потоки OAuth/OIDC). Держите сроки истечения короткими.</li>
        <li>Используйте <strong>сессионные токены</strong>, когда нужна возможность немедленного отзыва (выход,
        приостановка аккаунта, инциденты безопасности) или когда все ваши сервисы делят быстрое хранилище сессий.</li>
      </ul>

      <div style={{background: "rgba(239,68,68,0.08)", border: "1px solid rgba(239,68,68,0.3)", borderRadius: "12px", padding: "16px 20px", margin: "24px 0"}}>
        <strong>Критическое правило безопасности:</strong> всегда проверяйте подписи JWT на стороне сервера с
        помощью доверенного ключа. Никогда не полагайтесь только на проверку на стороне клиента. Клиент может
        декодировать полезную нагрузку любого JWT без секрета — но только сервер, владеющий правильным ключом, может
        определить, подлинна ли подпись и можно ли доверять токену. Декодирование на стороне клиента полезно лишь для{" "}
        <em>чтения</em> утверждений (например, чтобы показать имя пользователя в интерфейсе), но никогда для принятия
        решений об авторизации.
      </div>

      <h2>Как пользоваться декодировщиком JWT BrowseryTools</h2>
      <p>
        Откройте <a href="/tools/jwt-decoder">декодировщик JWT</a> и вставьте свой токен в поле ввода. Инструмент
        сразу разбивает токен по двум точкам и отображает:
      </p>
      <ul>
        <li>
          <strong>Панель заголовка:</strong> декодированный JSON, показывающий <code>alg</code>, <code>typ</code> и
          любые другие поля заголовка. Полезно для определения алгоритма подписи с одного взгляда.
        </li>
        <li>
          <strong>Панель полезной нагрузки:</strong> полный декодированный JSON со всеми утверждениями. Метки времени
          отображаются как в сыром Unix-формате, так и в читаемых человеком датах UTC, чтобы вы сразу видели
          истечение без расчётов в уме.
        </li>
        <li>
          <strong>Статус истечения:</strong> чёткий индикатор, показывающий, действителен ли токен сейчас, уже истёк
          или ещё не активен (на основе <code>nbf</code>). Если истёк, вы видите ровно, как давно это произошло.
        </li>
        <li>
          <strong>Сегмент подписи:</strong> сырая подпись в кодировке Base64URL, показанная для справки. Инструмент
          не проверяет подпись (для этого нужен секрет или открытый ключ), но декодирует и отображает всю информацию,
          нужную для отладки.
        </li>
      </ul>
      <p>
        Нет отправки формы, нет серверного запроса, нет доступа к буферу обмена сверх того, что вы явно вставляете.
        Разбор токена происходит полностью в JavaScript, работающем во вкладке вашего браузера.
      </p>

      <div style={{background: "rgba(34,197,94,0.08)", border: "1px solid rgba(34,197,94,0.3)", borderRadius: "12px", padding: "16px 20px", margin: "24px 0"}}>
        <strong>Ваши токены остаются приватными:</strong> JWT-токены часто содержат ID пользователей, e-mail, роли и
        другие персональные данные. Декодировщик JWT BrowseryTools обрабатывает ваш токен полностью в вашем браузере
        — он никогда не отправляется на сервер, не логируется и не хранится. Вы можете безопасно вставлять
        продакшен-токены для их изучения, не беспокоясь об утечке. Как только вы закроете вкладку, его не станет.
      </div>

      <h2>Декодируйте свой JWT-токен прямо сейчас</h2>
      <p>
        Отлаживаете ли вы истёкший токен, изучаете утверждения от OAuth-провайдера, проверяете, какие роли выданы
        пользователю, или просто пытаетесь понять, что на самом деле выдаёт ваша система аутентификации —{" "}
        <a href="/tools/jwt-decoder">декодировщик JWT BrowseryTools</a> даёт ответы мгновенно. Без регистрации, без
        установки расширений, без отправки данных куда-либо.
      </p>
      <div style={{background: "rgba(99,102,241,0.1)", border: "1px solid rgba(99,102,241,0.3)", borderRadius: "12px", padding: "20px 24px", margin: "32px 0", textAlign: "center" as const}}>
        <p style={{marginTop: 0, marginBottom: "16px", fontWeight: 600, fontSize: "1.05rem"}}>
          Бесплатный декодировщик JWT — мгновенно, приватно, без регистрации
        </p>
        <a
          href="/tools/jwt-decoder"
          style={{background: "rgba(99,102,241,0.9)", color: "white", padding: "10px 24px", borderRadius: "8px", textDecoration: "none", fontWeight: 500, fontSize: "0.95rem"}}
        >
          Открыть декодировщик JWT →
        </a>
      </div>
      <ToolCTA slug="jwt-decoder" variant="card" />
    </div>
  );
}
