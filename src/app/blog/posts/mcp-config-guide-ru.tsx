export default function Content() {
  return (
    <div>
      <p>
        На протяжении большей части короткой истории AI подключение языковой модели к внешнему
        инструменту означало написание кастомного кода интеграции для каждого инструмента.
        Хотите, чтобы модель читала файл? Напишите функцию. Делать запросы к базе данных?
        Напишите другую функцию, в другом формате, для каждой модели. Результатом стала
        фрагментированная экосистема, где каждое AI-приложение заново изобретало одни и те же
        базовые компоненты.
      </p>
      <p>
        Model Context Protocol (MCP) — ответ Anthropic на эту проблему: открытый стандарт,
        предоставляющий AI-моделям единый унифицированный интерфейс к инструментам, файлам,
        базам данных и сервисам. Воспользуйтесь{" "}
        <a href="/tools/mcp-config">Генератором MCP-конфигурации BrowseryTools</a> — бесплатно,
        без регистрации, всё остаётся в браузере — чтобы создавать и проверять файлы
        MCP-конфигурации без ручного написания JSON.
      </p>

      <h2>Что такое MCP и зачем он нужен?</h2>
      <p>
        MCP расшифровывается как Model Context Protocol. Это открытый протокол — опубликованный
        Anthropic в конце 2024 года и доступный на modelcontextprotocol.io — стандартизирующий
        взаимодействие AI-моделей с внешними источниками данных и инструментами. Думайте о нём
        как об универсальном адаптере: вместо того чтобы модели требовался отдельный плагин для
        GitHub, ещё один — для файловой системы и ещё один — для базы данных, MCP предоставляет
        единый интерфейс, которому может следовать любой совместимый клиент и сервер.
      </p>
      <p>
        Аналогия, которую использует Anthropic, — USB-C: до USB-C для каждого устройства
        требовался свой кабель. MCP стремится стать таким универсальным разъёмом для
        использования инструментов в AI. Инструмент, однажды созданный как MCP-сервер,
        работает с любым MCP-совместимым клиентом — Claude Desktop, Claude Code и любым
        другим хостом, реализующим протокол.
      </p>

      <h2>Архитектура MCP: клиенты, хосты и серверы</h2>
      <p>
        MCP включает три компонента, работающих совместно:
      </p>
      <ul>
        <li><strong>Хост</strong> — AI-приложение, запущенное на машине пользователя (например,
        Claude Desktop, расширение IDE). Хост управляет подключениями к одному или нескольким
        MCP-серверам и внедряет их возможности в AI-контекст.</li>
        <li><strong>Клиент</strong> — протокольный клиент, встроенный в хост и поддерживающий
        соединение 1:1 с одним MCP-сервером. Хост создаёт по одному клиенту на каждый сервер.</li>
        <li><strong>Сервер</strong> — лёгкая программа, предоставляющая возможности (инструменты,
        ресурсы, промпты) через протокол MCP. Серверы могут быть локальными процессами (запущенными
        на вашей машине) или удалёнными сервисами, доступными по HTTP.</li>
      </ul>
      <p>
        Когда вы просите Claude «прочитать README в моём проекте», MCP-клиент хоста отправляет
        запрос серверу MCP файловой системы, который читает файл и возвращает содержимое. Claude
        никогда не обращается к вашей файловой системе напрямую — это делает сервер, и он
        отчитывается через протокол.
      </p>

      <h2>Что могут предоставлять MCP-серверы</h2>
      <p>
        MCP-серверы могут предоставлять три типа возможностей:
      </p>
      <ul>
        <li><strong>Инструменты</strong> — функции, которые модель может вызывать. Примеры:
        поиск в базе данных, создание задачи на GitHub, выполнение команды в терминале, загрузка URL.</li>
        <li><strong>Ресурсы</strong> — данные, которые модель может читать. Примеры: файлы,
        строки из базы данных, ответы API, страницы документации. Ресурсы — как источники контекста
        только для чтения.</li>
        <li><strong>Промпты</strong> — готовые шаблоны промптов, которые пользователи могут
        вызывать по имени. Удобны для предоставления стандартизированных рабочих процессов.</li>
      </ul>

      <h2>Популярные MCP-серверы, которые стоит знать</h2>
      <ul>
        <li><strong>filesystem</strong> — читает и записывает файлы на локальной машине в указанных
        директориях. Наиболее часто используемый сервер. Необходим для чтения Claude Code вашей кодовой базы.</li>
        <li><strong>github</strong> — поиск по репозиториям, чтение файлов, создание задач и pull
        request'ов, получение истории коммитов. Использует GitHub API с вашим личным токеном доступа.</li>
        <li><strong>postgres / sqlite</strong> — выполняет SQL-запросы к базе данных. По умолчанию
        только на чтение в большинстве реализаций для безопасности.</li>
        <li><strong>brave-search / fetch</strong> — загружает URL или выполняет веб-поиск,
        предоставляя модели доступ к актуальной информации за пределами обучающих данных.</li>
        <li><strong>memory</strong> — постоянное хранилище ключ-значение, сохраняющееся между
        сессиями. Даёт модели слой памяти, в который она может писать и из которого может читать.</li>
        <li><strong>puppeteer / playwright</strong> — управляет headless-браузером. Позволяет
        модели навигировать по веб-страницам, заполнять формы и извлекать контент с сайтов,
        отрендеренных на JavaScript.</li>
      </ul>

      <h2>Написание базового MCP Config JSON</h2>
      <p>
        Конфигурация MCP хранится в JSON-файле, который хост-приложение читает при запуске. Для
        Claude Desktop на macOS этот файл находится по адресу{" "}
        <code>~/Library/Application Support/Claude/claude_desktop_config.json</code>. Структура
        одинакова независимо от того, какие серверы вы добавляете:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/yourname/Documents",
        "/Users/yourname/Projects"
      ]
    }
  }
}`}
      </pre>
      <p>
        Каждый ключ внутри <code>mcpServers</code> — это имя, которое вы даёте серверу: именно
        оно отображается в интерфейсе Claude. Поля <code>command</code> и <code>args</code>
        определяют, как запустить серверный процесс. Большинство официальных серверов — npm-пакеты,
        поэтому <code>npx -y</code> загружает и запускает их при первом использовании без
        отдельного шага установки.
      </p>

      <h2>Добавление нескольких серверов</h2>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/yourname/Projects"
      ]
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_your_token_here"
      }
    },
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://localhost/mydb"
      ]
    }
  }
}`}
      </pre>
      <p>
        Поле <code>env</code> передаёт переменные окружения серверному процессу.
        Конфиденциальные значения — API-ключи и учётные данные баз данных — должны передаваться
        здесь, а не жёстко указываться в <code>args</code>, чтобы ими можно было управлять
        отдельно и избежать случайного добавления в систему контроля версий.
      </p>

      <h2>Настройка MCP в Claude Code</h2>
      <p>
        Claude Code (CLI-инструмент) использует немного другой механизм конфигурации. Серверы
        MCP добавляются командой <code>claude mcp add</code>:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`# Add a local stdio server
claude mcp add filesystem -- npx -y @modelcontextprotocol/server-filesystem /path/to/dir

# Add a remote HTTP server
claude mcp add my-server --transport http http://localhost:8080/mcp

# Add with environment variables
claude mcp add github -e GITHUB_PERSONAL_ACCESS_TOKEN=ghp_... -- npx -y @modelcontextprotocol/server-github

# List all configured servers
claude mcp list`}
      </pre>
      <p>
        Claude Code хранит конфигурации серверов в <code>~/.claude/</code> по умолчанию
        (область пользователя) или в <code>.mcp.json</code> в корне проекта (область проекта).
        Конфигурации области проекта удобны для командных репозиториев — закоммитьте{" "}
        <code>.mcp.json</code>, и все в команде автоматически получат одинаковую конфигурацию
        серверов.
      </p>

      <h2>Типичные ошибки конфигурации</h2>
      <ul>
        <li><strong>Неправильный разделитель пути</strong> — Windows использует обратные косые
        черты, но строки JSON требуют прямых косых черт или экранированных обратных. Всегда
        используйте прямые косые черты в MCP-конфигах, даже на Windows.</li>
        <li><strong>Отсутствие прав на директорию</strong> — сервер файловой системы может
        обращаться только к директориям, явно указанным в его аргументах. Если Claude говорит,
        что не может найти файл, проверьте, включена ли родительская директория файла в
        разрешённый список.</li>
        <li><strong>Зависший серверный процесс</strong> — при сбое сервера хост может не
        перезапустить его автоматически. Перезапустите Claude Desktop или выполните{" "}
        <code>claude mcp restart &lt;name&gt;</code> в Claude Code для свежего подключения.</li>
        <li><strong>Несовпадение версий</strong> — MCP активно развивается. Если сервер ведёт
        себя неожиданно, проверьте, запущена ли последняя версия с помощью{" "}
        <code>npx -y @modelcontextprotocol/server-name@latest</code>.</li>
      </ul>

      <h2>Генерируйте конфиг с помощью BrowseryTools</h2>
      <p>
        Писать MCP JSON вручную утомительно и легко допустить ошибку — пропущенная запятая
        или неправильно закавыченный путь молча сломает всю конфигурацию.{" "}
        <a href="/tools/mcp-config">Генератор MCP-конфигурации BrowseryTools</a> позволяет
        выбрать серверы, заполнить необходимые параметры и получить валидный, отформатированный
        JSON-конфиг, готовый к вставке в файл конфигурации Claude Desktop или{" "}
        <code>.mcp.json</code>. Всё работает в браузере, учётные данные не хранятся.
      </p>

      <h2>Итоги</h2>
      <p>
        MCP — это инфраструктурный слой, который превращает автономную чат-модель в
        подключённого агента с доступом к вашим реальным файлам, коду, базам данных и сервисам.
        Протокол открытый, серверы модульные, формат конфигурации — простой JSON. После того
        как MCP-конфигурация настроена, вы получаете значительно более мощного AI-ассистента
        без изменения способа взаимодействия с ним — инструменты просто доступны и готовы
        к использованию.
      </p>
    </div>
  );
}
