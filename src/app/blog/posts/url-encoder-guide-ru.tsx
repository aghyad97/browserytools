import { ToolCTA } from "@/components/blog/ToolCTA";

export default function Content() {
  return (
    <div>
      <p>
        URL выглядят просто снаружи — строка текста, указывающая на ресурс. Но под капотом они
        подчиняются строгой грамматике, допускающей лишь определённый набор символов. Стоит попытаться
        передать в URL пробел, амперсанд или символ не из ASCII без кодирования — и возникают ошибки,
        которые бывает сложно отладить. Процентное кодирование (URL-кодирование) — это механизм,
        позволяющий безопасно встраивать произвольные данные в URL.
      </p>
      <ToolCTA slug="url-encoder" variant="inline" />
      <p>
        Вы можете мгновенно кодировать и декодировать URL с помощью{" "}
        <a href="/tools/url-encoder">кодировщика/декодировщика URL BrowseryTools</a> — бесплатно,
        без регистрации, всё остаётся в браузере.
      </p>

      <h2>Почему специальные символы ломают URL</h2>
      <p>
        Спецификация URL (RFC 3986) резервирует определённые символы для структурных целей. Знак{" "}
        <code>?</code> отделяет путь от строки запроса. Знак <code>&amp;</code> разделяет параметры
        запроса друг от друга. Знак <code>#</code> обозначает идентификатор фрагмента. Знак{" "}
        <code>/</code> разделяет сегменты пути. Если ваши данные содержат любой из этих символов,
        парсер URL не сможет отличить ваши данные от структуры самого URL.
      </p>
      <p>
        Рассмотрим поисковый запрос <code>rock &amp; roll</code>. При наивном построении URL получается:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`/search?q=rock & roll
          ^     ^
          |     └── похоже, здесь начинается новый параметр
          └── этот & делит q и мнимый второй параметр`}
      </pre>
      <p>
        Парсер читает <code>q=rock </code> (с пробелом в конце) как первый параметр, затем встречает
        то, что похоже на начало второго параметра с именем <code> roll</code>. Оба значения неверны.
        Правильный URL: <code>/search?q=rock%20%26%20roll</code> — пробел становится <code>%20</code>,
        а амперсанд — <code>%26</code>.
      </p>

      <h2>Как работает процентное кодирование</h2>
      <p>
        Процентное кодирование преобразует байт в трёхсимвольную последовательность: знак процента
        и две заглавные шестнадцатеричные цифры, представляющие значение байта. Символ пробела
        (байт ASCII 32, шестнадцатеричный <code>0x20</code>) становится <code>%20</code>. Символ
        «собачки» (<code>@</code>, ASCII 64, <code>0x40</code>) становится <code>%40</code>. Правило:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`percent-encode(byte) = "%" + byte.toString(16).toUpperCase().padStart(2, "0")

Примеры:
  пробел (0x20) → %20
  @      (0x40) → %40
  [      (0x5B) → %5B
  €      (UTF-8: 0xE2 0x82 0xAC) → %E2%82%AC`}
      </pre>
      <p>
        Для многобайтовых Unicode-символов (всё, что выходит за пределы ASCII) символ сначала
        кодируется в байты UTF-8, а затем каждый байт кодируется процентным кодированием. Знак
        евро <code>€</code> — это три байта UTF-8, поэтому он превращается в три закодированных
        последовательности: <code>%E2%82%AC</code>.
      </p>

      <h2>Безопасные символы и зарезервированные символы</h2>
      <p>
        Не каждый символ нуждается в кодировании. RFC 3986 определяет два набора, безопасных для
        использования как есть:
      </p>
      <ul>
        <li><strong>Незарезервированные символы</strong> — A–Z, a–z, 0–9, дефис, подчёркивание, точка, тильда. Они не имеют специального смысла и никогда не требуют кодирования.</li>
        <li><strong>Зарезервированные символы</strong> — <code>: / ? # [ ] @ ! $ &amp; ' ( ) * + , ; =</code>. Они безопасны в своих структурных позициях, но должны кодироваться при появлении в значениях данных.</li>
      </ul>
      <p>
        Всё остальное — пробелы, Unicode, управляющие символы, большинство знаков препинания — должно
        всегда кодироваться.
      </p>

      <h2>encodeURI против encodeURIComponent: критическое различие</h2>
      <p>
        JavaScript поставляется с двумя встроенными функциями кодирования, и путаница между ними
        является одной из самых распространённых ошибок URL-кодирования в веб-приложениях.
      </p>
      <p>
        <code>encodeURI()</code> предназначен для кодирования полного URL. Он оставляет все
        зарезервированные символы нетронутыми, поскольку они структурно значимы в полном URL.
        Используйте его, если у вас есть полный URL, который может содержать пробелы или Unicode,
        но имеет правильную структуру:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`encodeURI("https://example.com/search?q=hello world&lang=en")
// → "https://example.com/search?q=hello%20world&lang=en"
//   ✓ пробел закодирован, & и ? остались нетронутыми`}
      </pre>
      <p>
        <code>encodeURIComponent()</code> предназначен для кодирования одного значения — значения
        параметра запроса, сегмента пути, любых данных. Он кодирует и зарезервированные символы,
        включая <code>&amp;</code>, <code>=</code>, <code>?</code> и <code>/</code>:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`encodeURIComponent("rock & roll")
// → "rock%20%26%20roll"
//   ✓ & закодирован — безопасно использовать как значение параметра запроса

encodeURIComponent("https://example.com/page")
// → "https%3A%2F%2Fexample.com%2Fpage"
//   ✓ двоеточия и слэши закодированы — безопасно как значение redirect_uri`}
      </pre>
      <p>
        Практическое правило: при построении URL используйте <code>encodeURIComponent()</code> для
        каждого отдельного значения параметра, но не для всего URL. Используйте <code>encodeURI()</code>
        только для полного URL, который нужно нормализовать. В современном коде предпочитайте API{" "}
        <code>URL</code> и <code>URLSearchParams</code> вместо ручного кодирования — они обрабатывают
        кодирование автоматически и корректно.
      </p>

      <h2>Подводные камни кодирования строки запроса</h2>
      <p>
        При кодировании строк запроса регулярно встречаются несколько тонких багов. Знак <code>+</code>{" "}
        заслуживает особого внимания: в формате <code>application/x-www-form-urlencoded</code> (формат,
        в котором HTML-формы отправляют данные) пробел кодируется как <code>+</code>, а не{" "}
        <code>%20</code>. Это устаревшее соглашение, предшествующее RFC 3986. Если бэкенд декодирует
        URL по правилам form-encoding, а фронтенд отправляет <code>%20</code> — всё работает. Но если
        фронтенд отправляет <code>+</code>, а бэкенд декодирует по правилам RFC 3986, то <code>+</code>{" "}
        остаётся буквальным знаком плюс, а не пробелом.
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// URLSearchParams использует application/x-www-form-urlencoded (+ для пробелов)
new URLSearchParams({ q: "rock & roll" }).toString()
// → "q=rock+%26+roll"

// encodeURIComponent использует RFC 3986 (%20 для пробелов)
"q=" + encodeURIComponent("rock & roll")
// → "q=rock%20%26%20roll"

// Оба варианта правильны — главное, чтобы оба конца были согласованы`}
      </pre>

      <h2>Как данные формы кодируются в URL</h2>
      <p>
        Когда HTML-форма отправляется с <code>method="GET"</code>, браузер сериализует поля формы
        в строку запроса, используя <code>application/x-www-form-urlencoded</code>. Каждое имя
        и значение поля кодируется (пробелы как <code>+</code>, специальные символы как{" "}
        <code>%XX</code>), а поля объединяются через <code>&amp;</code>. Для форм с{" "}
        <code>method="POST"</code> без атрибута <code>enctype</code> используется то же кодирование,
        но данные помещаются в тело запроса, а не в URL.
      </p>
      <p>
        Это также формат, который <code>fetch()</code> использует при передаче объекта{" "}
        <code>URLSearchParams</code> в качестве тела, и это то, что большинство серверных фреймворков
        автоматически декодируют при чтении отправок форм.
      </p>

      <h2>Base64 в URL</h2>
      <p>
        Стандартный Base64 использует <code>+</code> и <code>/</code> — оба символа имеют специальный
        смысл в URL. Когда Base64-кодированные данные должны появляться в URL (распространённый паттерн
        для токенов, данных изображений или криптографических подписей), вместо них используйте вариант
        Base64URL. Он заменяет <code>+</code> на <code>-</code>, а <code>/</code> на <code>_</code>,
        создавая строку, безопасную в любой позиции URL без дополнительного кодирования. JWT использует
        этот формат для сегментов заголовка и полезной нагрузки.
      </p>

      <h2>Реальные баги с кодированием</h2>
      <p>
        Несколько паттернов багов, встречающихся в продакшн-приложениях:
      </p>
      <ul>
        <li><strong>Двойное кодирование</strong> — кодирование уже закодированного URL. <code>%20</code> становится <code>%2520</code>, потому что сам <code>%</code> кодируется в <code>%25</code>. Всегда проверяйте, не закодировано ли значение уже, прежде чем кодировать его снова.</li>
        <li><strong>Отсутствие encodeURIComponent для redirect_uri</strong> — OAuth-потоки передают <code>redirect_uri</code> как параметр запроса. Если он содержит <code>?</code> или <code>&amp;</code> и не закодирован, сервер авторизации разбирает эти символы как часть структуры внешнего URL, нарушая редирект.</li>
        <li><strong>Не-UTF-8 кодирование</strong> — старые системы или неправильно настроенные серверы иногда кодируют строки, используя ISO-8859-1 вместо UTF-8. Байтовые последовательности для <code>é</code> различаются. Всегда единообразно используйте UTF-8 с обеих сторон.</li>
        <li><strong>Логирование сырых URL</strong> — запись URL, содержащего закодированные пользовательские данные, может давать вводящие в заблуждение логи, если ваш просмотрщик логов автоматически декодирует процентные последовательности, скрывая то, что реально было передано по сети.</li>
      </ul>

      <h2>Мгновенное кодирование и декодирование URL</h2>
      <p>
        Отлаживаете ли вы OAuth-редирект, вручную строите строку запроса, проверяете сломанный запрос
        к API или просто хотите понять, что скрывается за процентно-закодированным URL —{" "}
        <a href="/tools/url-encoder">кодировщик/декодировщик URL BrowseryTools</a> обработает это
        мгновенно. Вставьте строку, выберите кодирование или декодирование, и сразу увидите результат.
        Без серверных запросов, без регистрации.
      </p>
      <div style={{background: "rgba(99,102,241,0.1)", border: "1px solid rgba(99,102,241,0.3)", borderRadius: "12px", padding: "20px 24px", margin: "32px 0", textAlign: "center" as const}}>
        <p style={{marginTop: 0, marginBottom: "16px", fontWeight: 600, fontSize: "1.05rem"}}>
          Бесплатный кодировщик/декодировщик URL — работает на 100% в браузере
        </p>
        <a
          href="/tools/url-encoder"
          style={{background: "rgba(99,102,241,0.9)", color: "white", padding: "10px 24px", borderRadius: "8px", textDecoration: "none", fontWeight: 500, fontSize: "0.95rem"}}
        >
          Открыть кодировщик URL →
        </a>
      </div>
      <ToolCTA slug="url-encoder" variant="card" />
    </div>
  );
}
