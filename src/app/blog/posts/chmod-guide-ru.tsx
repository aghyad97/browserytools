export default function Content() {
  return (
    <div>
      <p>
        Каждый файл и каталог в Linux или macOS имеет набор прав доступа, управляющих тем, кто может
        читать его, записывать в него или исполнять его. Правильная настройка этих прав — разница
        между защищённым сервером и тем, что утечёт данные или окажется скомпрометированным. Тем не
        менее запись вроде <code>chmod 755</code> или вывод <code>ls -la</code> с{" "}
        <code>-rwxr-xr--</code> могут казаться непрозрачными, пока вы не поймёте лежащую в основе
        модель. Это руководство объясняет права доступа Unix с первых принципов.
      </p>
      <p>
        Вы можете вычислять значения прав и конвертировать между восьмеричной и символьной нотацией
        с помощью{" "}
        <a href="/tools/chmod">калькулятора chmod BrowseryTools</a> — бесплатно, без регистрации,
        всё работает в браузере.
      </p>

      <h2>Модель прав Unix: владелец, группа, остальные</h2>
      <p>
        Unix назначает каждому файлу и каталогу три набора прав, каждый из которых охватывает
        определённую аудиторию:
      </p>
      <ul>
        <li><strong>Владелец (user)</strong> — учётная запись пользователя, которому принадлежит файл. Как правило, пользователь, создавший его.</li>
        <li><strong>Группа</strong> — именованная группа пользователей. Файл принадлежит одной группе; все члены этой группы разделяют групповые права.</li>
        <li><strong>Остальные (other/world)</strong> — все остальные пользователи системы, кроме владельца и членов группы.</li>
      </ul>
      <p>
        Внутри каждого из трёх наборов есть три бита прав: чтение (<code>r</code>), запись (<code>w</code>)
        и выполнение (<code>x</code>). Итого девять битов прав, которые напрямую соответствуют девяти
        символам после индикатора типа файла в выводе <code>ls -la</code>.
      </p>

      <h2>Чтение вывода ls -la</h2>
      <p>
        При запуске <code>ls -la</code> каждая строка начинается с 10-символьной строки вроде{" "}
        <code>-rwxr-xr--</code>. Вот как её читать:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`-  rwx  r-x  r--
^  ^^^  ^^^  ^^^
|  |    |    └── остальные: только чтение
|  |    └─────── группа:    чтение + выполнение
|  └──────────── владелец:  чтение + запись + выполнение
└─────────────── тип файла: - = файл, d = каталог, l = символическая ссылка`}
      </pre>
      <p>
        Дефис <code>-</code> на месте бита права означает, что это право не предоставлено. Таким
        образом, <code>r-x</code> означает: чтение и выполнение разрешены, запись — нет.
      </p>

      <h2>Смысл чтения, записи и выполнения для файлов и каталогов</h2>
      <p>
        Три бита прав имеют разный смысл в зависимости от того, применяются ли они к файлу или
        каталогу:
      </p>
      <ul>
        <li><strong>Чтение файла (r)</strong> — можно читать содержимое файла (<code>cat</code>, <code>less</code>, открыть в редакторе).</li>
        <li><strong>Запись файла (w)</strong> — можно изменять или усекать файл. Удаление файла контролируется правом на запись родительского каталога, а не битом записи самого файла.</li>
        <li><strong>Выполнение файла (x)</strong> — можно запускать файл как программу или скрипт. Без этого бита <code>./script.sh</code> возвращает «Permission denied», даже если файл можно читать.</li>
        <li><strong>Чтение каталога (r)</strong> — можно просматривать содержимое каталога (<code>ls</code>). Без него каталог существует, но его содержимое невидимо.</li>
        <li><strong>Запись каталога (w)</strong> — можно создавать, переименовывать или удалять файлы внутри каталога. Поэтому можно удалить файл, которым не владеешь, если есть право записи в родительский каталог.</li>
        <li><strong>Выполнение каталога (x)</strong> — можно входить в каталог (<code>cd</code>) и обращаться к файлам внутри, зная их имена. Иногда называется «битом поиска». Каталог с <code>r--</code> позволяет перечислять имена файлов, но не обращаться к ним; каталог с <code>--x</code> позволяет обращаться к файлам по имени, но не перечислять их.</li>
      </ul>

      <h2>Восьмеричная нотация: 755, 644, 777</h2>
      <p>
        Каждый набор прав (владелец, группа, остальные) — три бита. Три бита могут представлять
        значения от 0 до 7 — ровно одна восьмеричная цифра. Именно поэтому права записываются как
        три восьмеричные цифры, по одной для каждой аудитории:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`Значения битов:  r = 4,  w = 2,  x = 1

rwx = 4+2+1 = 7
r-x = 4+0+1 = 5
r-- = 4+0+0 = 4
--- = 0+0+0 = 0

chmod 755 → владелец: 7 (rwx), группа: 5 (r-x), остальные: 5 (r-x)
chmod 644 → владелец: 6 (rw-), группа: 4 (r--), остальные: 4 (r--)
chmod 600 → владелец: 6 (rw-), группа: 0 (---), остальные: 0 (---)`}
      </pre>
      <p>
        Запоминать все комбинации необязательно — используйте{" "}
        <a href="/tools/chmod">калькулятор chmod BrowseryTools</a>, чтобы проверить смысл любого
        восьмеричного значения или подобрать нужное для вашей ситуации.
      </p>

      <h2>Символьная нотация: u+x, g-w, o=r</h2>
      <p>
        Символьный режим позволяет изменять права относительно их текущего состояния, не задавая
        все три набора сразу. Формат: <code>[кто][операция][права]</code>:
      </p>
      <ul>
        <li><strong>Кто</strong>: <code>u</code> (владелец/пользователь), <code>g</code> (группа), <code>o</code> (остальные), <code>a</code> (все трое)</li>
        <li><strong>Операция</strong>: <code>+</code> (добавить), <code>-</code> (убрать), <code>=</code> (задать точно)</li>
        <li><strong>Права</strong>: <code>r</code>, <code>w</code>, <code>x</code></li>
      </ul>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`chmod u+x script.sh       # добавить выполнение только для владельца
chmod g-w config.txt      # убрать запись для группы
chmod o=r public.html     # задать остальным только чтение
chmod a+r file.txt        # добавить чтение для всех
chmod u=rwx,g=rx,o=       # эквивалентно chmod 750`}
      </pre>

      <h2>Типичные паттерны прав: разбор</h2>
      <ul>
        <li><strong>755</strong> (<code>rwxr-xr-x</code>) — стандарт для исполняемых файлов и каталогов. Владелец может всё; все остальные могут читать и выполнять (или входить в каталог), но не записывать. По умолчанию для корневых каталогов документов веб-сервера и публичных скриптов.</li>
        <li><strong>644</strong> (<code>rw-r--r--</code>) — стандарт для обычных файлов. Владелец может читать и писать; все остальные только читают. Подходит для веб-ресурсов, конфигурационных файлов без секретов и большинства статического контента.</li>
        <li><strong>600</strong> (<code>rw-------</code>) — владелец может читать и писать; никто другой не имеет доступа. Обязателен для приватных SSH-ключей (<code>~/.ssh/id_rsa</code>). SSH откажется использовать файл ключа с более слабыми правами.</li>
        <li><strong>700</strong> (<code>rwx------</code>) — владелец может всё; никто другой не имеет доступа. Хорошо для приватных скриптов и каталогов с конфиденциальными данными.</li>
        <li><strong>400</strong> (<code>r--------</code>) — только чтение для владельца; полная блокировка для всех остальных. Используется для неизменяемых конфигурационных файлов и сертификатов, где случайная запись была бы опасна.</li>
      </ul>

      <h2>Почему 777 опасен</h2>
      <p>
        <code>chmod 777</code> даёт права на чтение, запись и выполнение каждому пользователю в
        системе. Это означает, что любой процесс, запущенный от любого пользователя — включая
        скомпрометированное веб-приложение, вредоносный скрипт в общем хостинге или любого другого
        пользователя на машине — может изменить или исполнить файл. В контексте веб-сервера PHP-файл
        с правами 777 позволяет любому другому процессу перезаписать его вредоносным кодом. Никогда
        не используйте 777 в продакшне. Если вы используете его для «исправления ошибки прав»,
        правильное решение — передать файл нужному пользователю или группе.
      </p>

      <h2>Setuid, Setgid и Sticky bit</h2>
      <p>
        Помимо девяти стандартных битов, существуют три специальных бита, отображаемых как четвёртая
        ведущая цифра в четырёхзначной восьмеричной нотации:
      </p>
      <ul>
        <li><strong>Setuid (4xxx)</strong> — при установке на исполняемый файл программа запускается с привилегиями владельца файла, а не вызывающего. <code>/usr/bin/passwd</code> использует это, чтобы обычные пользователи могли писать в <code>/etc/shadow</code>, принадлежащий root.</li>
        <li><strong>Setgid (2xxx)</strong> — на исполняемом файле запускается с привилегиями группы файла. На каталоге: новые файлы, созданные внутри, наследуют группу каталога, а не основную группу создателя — полезно для каталогов совместных проектов.</li>
        <li><strong>Sticky bit (1xxx)</strong> — на каталоге запрещает пользователям удалять файлы, которыми они не владеют, даже если есть право записи в каталог. На <code>/tmp</code> установлен sticky bit (<code>chmod 1777</code>), чтобы пользователи могли создавать свои временные файлы, но не могли удалять чужие.</li>
      </ul>

      <h2>chmod с флагом -R и практические примеры</h2>
      <p>
        Флаг <code>-R</code> рекурсивно применяет изменение прав к каталогу и всему его содержимому.
        Используйте его осторожно — применение одних и тех же прав и к файлам, и к каталогам часто
        неверно, потому что каталогам нужен бит выполнения для входа в них, а обычные файлы в норме
        не должны иметь права выполнения:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`# Веб-сервер: каталогам нужны 755, файлам — 644
find /var/www/html -type d -exec chmod 755 {} \;
find /var/www/html -type f -exec chmod 644 {} \;

# Исправить права SSH-ключей
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub
chmod 644 ~/.ssh/authorized_keys

# Сделать скрипт деплоя исполняемым
chmod +x deploy.sh`}
      </pre>
      <p>
        Если вы не уверены, какое восьмеричное значение использовать,{" "}
        <a href="/tools/chmod">калькулятор chmod BrowseryTools</a> позволяет отмечать чекбоксы прав
        для владельца, группы и остальных и сразу видеть результирующее восьмеричное значение
        и символьную нотацию.
      </p>
      <div style={{background: "rgba(99,102,241,0.1)", border: "1px solid rgba(99,102,241,0.3)", borderRadius: "12px", padding: "20px 24px", margin: "32px 0", textAlign: "center" as const}}>
        <p style={{marginTop: 0, marginBottom: "16px", fontWeight: 600, fontSize: "1.05rem"}}>
          Бесплатный калькулятор chmod — восьмеричное ↔ символьное ↔ читаемое
        </p>
        <a
          href="/tools/chmod"
          style={{background: "rgba(99,102,241,0.9)", color: "white", padding: "10px 24px", borderRadius: "8px", textDecoration: "none", fontWeight: 500, fontSize: "0.95rem"}}
        >
          Открыть калькулятор chmod →
        </a>
      </div>
    </div>
  );
}
