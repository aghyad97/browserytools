export default function Content() {
  return (
    <div>
      <p>
        Откройте любой лог-файл. Посмотрите на поле expiry в JWT-токене. Проверьте поле{" "}
        <code>created_at</code>{" "}в ответе API. Скорее всего, вы встретите число вроде{" "}
        <code>1711065600</code> или <code>1711065600000</code>. Это Unix-метка времени —
        простое целое число, представляющее момент во времени. Понимание того, как работает
        Unix-время, откуда оно берётся и как избежать типичных ловушек, избавит вас от целого
        класса ошибок — незаметных, трудновоспроизводимых и порой весьма неловких в продакшене.
      </p>
      <p>
        Вы можете конвертировать любую Unix-метку в читаемую дату (и обратно) с помощью{" "}
        <a href="/tools/unix-timestamp">конвертера Unix-меток BrowseryTools</a> — бесплатно,
        без регистрации, всё остаётся в вашем браузере.
      </p>

      <h2>Что такое Unix-метка времени?</h2>
      <p>
        Unix-метка времени — это количество секунд, прошедших с начала эпохи Unix: полуночи
        1 января 1970 года по всемирному координированному времени (UTC). Этот момент —
        00:00:00 UTC 1970-01-01 — был выбран в качестве точки отсчёта при разработке
        операционной системы Unix в начале 1970-х годов. Это была недавняя, «круглая» дата,
        удобная для вычислений на тогдашнем железе.
      </p>
      <p>
        Элегантность Unix-времени в том, что любой момент представлен единственным целым числом.
        Сравнение двух меток — это вычитание. Проверка истечения срока — сравнение. Добавление
        интервала — сложение. Никаких часовых поясов, никаких календарных вычислений, никакого
        перехода на летнее время — только число.
      </p>
      <p>
        По состоянию на 2026 год текущая Unix-метка составляет примерно{" "}
        <code>1 774 000 000</code>. Каждую секунду это число увеличивается на 1.
      </p>

      <h2>Проблема Y2K38</h2>
      <p>
        Если Unix-время хранится как 32-битное знаковое целое число — а так и было во многих
        ранних реализациях — максимальное значение равно <code>2 147 483 647</code>. Это число
        соответствует 03:14:07 UTC 19 января 2038 года. После этого момента 32-битное знаковое
        целое переполняется и становится большим отрицательным числом, а системы, которые не
        были обновлены, начнут неверно интерпретировать метки времени.
      </p>
      <p>
        Это проблема 2038 года (Y2K38) — Unix-эра аналог ошибки Y2K. Современные системы
        используют 64-битные целые для меток времени, что расширяет представимый диапазон
        примерно до 292 миллиардов лет в обоих направлениях — практически навсегда для любых
        реальных задач. Но встроенные системы, устаревшие базы данных с 32-битными столбцами
        меток и старый C-код, использующий <code>time_t</code> как 32-битный тип, всё ещё
        подвержены риску.
      </p>

      <h2>Получение текущей метки времени</h2>
      <p>
        Вот как получить текущую Unix-метку на наиболее популярных языках:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// JavaScript — возвращает миллисекунды, разделите на 1000 для секунд
const nowMs = Date.now();           // e.g. 1711065600000
const nowSec = Math.floor(Date.now() / 1000);  // e.g. 1711065600

// Python
import time
now = int(time.time())  # seconds since epoch

# Using datetime module
from datetime import datetime, timezone
now = int(datetime.now(timezone.utc).timestamp())

// Go
import "time"
now := time.Now().Unix()         // seconds
nowNano := time.Now().UnixNano() // nanoseconds

-- SQL (PostgreSQL)
SELECT EXTRACT(EPOCH FROM NOW())::BIGINT;

-- SQL (MySQL)
SELECT UNIX_TIMESTAMP();`}
      </pre>

      <h2>Конвертация меток в читаемые даты</h2>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// JavaScript — из секунд
const ts = 1711065600;
const date = new Date(ts * 1000);          // multiply by 1000 for ms
console.log(date.toISOString());            // "2024-03-22T00:00:00.000Z"
console.log(date.toLocaleDateString());    // locale-formatted date

// Python
import datetime
ts = 1711065600
dt = datetime.datetime.fromtimestamp(ts, tz=datetime.timezone.utc)
print(dt.isoformat())  # 2024-03-22T00:00:00+00:00

-- PostgreSQL: timestamp from integer
SELECT to_timestamp(1711065600);
-- Result: 2024-03-22 00:00:00+00

-- MySQL
SELECT FROM_UNIXTIME(1711065600);
-- Result: 2024-03-22 00:00:00`}
      </pre>

      <h2>Ошибка №1 с метками: миллисекунды против секунд</h2>
      <p>
        Метод <code>Date.now()</code> в JavaScript возвращает миллисекунды. Стандарт Unix — и
        практически все остальные языки, базы данных и API — использует секунды. Это
        несоответствие — самый распространённый источник ошибок с метками времени.
      </p>
      <p>
        Симптомы узнаваемы: даты отображаются как 1970 год (метка случайно делится на 1000, или
        миллисекундная метка трактуется как секундная), либо даты попадают в 56000+ год
        (секундная метка трактуется как миллисекундная и затем снова делится). Значение около{" "}
        <code>1 700 000 000</code> — почти наверняка секунды. Значение около{" "}
        <code>1 700 000 000 000</code> — почти наверняка миллисекунды.
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// Bug: treating seconds as milliseconds — lands in 1970
new Date(1711065600)        // Mon Jan 20 1970 11:24:25 UTC 🚫

// Correct: multiply seconds by 1000
new Date(1711065600 * 1000) // Fri Mar 22 2024 00:00:00 UTC ✓

// Defensive helper — handles both seconds and milliseconds
function toDate(ts) {
  // If it's under 10^12, it's seconds; multiply
  return new Date(ts < 1e12 ? ts * 1000 : ts);
}`}
      </pre>

      <h2>Проблемы часовых поясов с метками времени</h2>
      <p>
        Unix-метки всегда в UTC — они представляют единственный абсолютный момент во времени,
        без привязки к часовому поясу. Вопрос часового пояса возникает только на уровне
        отображения, когда метка конвертируется в читаемый формат.
      </p>
      <p>
        Самая распространённая ошибка — использование методов локального часового пояса без
        осознания этого. <code>new Date(ts).toLocaleDateString()</code> в JavaScript возвращает
        дату в локальном часовом поясе браузера. Если сервер генерирует метку в 23:00 UTC, а
        пользователь в UTC+0 и пользователь в UTC+1 оба её отображают, они увидят разные
        календарные даты. Правильно это или нет — зависит от требований продукта, но это должно
        быть осознанным выбором, а не случайностью.
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// Always explicit about timezone — use toISOString() for UTC
const date = new Date(1711065600 * 1000);
date.toISOString()        // "2024-03-22T00:00:00.000Z"  ← always UTC

// Or use Intl.DateTimeFormat for locale/timezone display
new Intl.DateTimeFormat("en-US", {
  timeZone: "America/New_York",
  dateStyle: "full",
}).format(date);  // "Friday, March 22, 2024"`}
      </pre>

      <h2>Метки времени в базах данных</h2>
      <p>
        Базы данных предлагают два основных варианта хранения дат: тип столбца{" "}
        <code>TIMESTAMP</code> (хранит абсолютный момент во времени) и тип{" "}
        <code>DATE</code> или <code>DATETIME</code>{" "}(хранит календарное представление без
        inherent часового пояса).
      </p>
      <p>
        Для полей вроде <code>created_at</code>, <code>updated_at</code> и меток событий
        всегда используйте столбец <code>TIMESTAMP WITH TIME ZONE</code> (или его аналог в
        вашей СУБД), а не целое число. Это позволяет базе данных корректно обрабатывать
        конвертацию часовых поясов и сравнения, а запросы вроде «события за последние 24 часа»
        работают правильно независимо от настроек часового пояса сервера.
      </p>
      <p>
        Если всё же нужно хранить Unix-метку как «сырое» целое (для совместимости со
        сторонними системами или максимальной переносимости), чётко задокументируйте, секунды
        это или миллисекунды, и будьте последовательны во всей схеме.
      </p>

      <h2>Метки времени в JWT и API</h2>
      <p>
        JSON Web Tokens (JWT) используют Unix-метки (в секундах) для временны́х клеймов:
      </p>
      <ul>
        <li><strong><code>iat</code></strong> — issued at: время создания токена</li>
        <li><strong><code>exp</code></strong> — expiry: время, после которого токен не должен приниматься</li>
        <li><strong><code>nbf</code></strong> — not before: токен не должен использоваться до этого момента</li>
      </ul>
      <p>
        Проверка истечения JWT — это простое сравнение:{" "}
        <code>exp &gt; Math.floor(Date.now() / 1000)</code>. Если текущее время в секундах
        больше <code>exp</code>, токен истёк. Всегда проверяйте <code>exp</code> на сервере —
        никогда не полагайтесь только на клиентскую проверку срока.
      </p>

      <h2>Быстрая справка: конвертация меток</h2>
      <p>
        Для быстрой и точной конвертации между Unix-метками и читаемыми датами используйте{" "}
        <a href="/tools/unix-timestamp">конвертер Unix-меток BrowseryTools</a>. Вставьте метку,
        чтобы увидеть соответствующую UTC и локальную дату, или введите дату, чтобы получить
        метку. Всё работает в браузере — без сервера, без отслеживания.
      </p>

      <h2>Итог</h2>
      <p>
        Unix-метки времени — универсальный и однозначный способ представления моментов во
        времени. Ключевые правила: они всегда в UTC, всегда в секундах (кроме JavaScript, где{" "}
        <code>Date.now()</code> использует миллисекунды) и всегда являются положительным целым
        для любой даты после 1970 года. Явно обрабатывайте различие миллисекунд и секунд,
        используйте UTC для хранения и передачи, и конвертируйте в местное время только на
        уровне отображения.
      </p>
    </div>
  );
}
