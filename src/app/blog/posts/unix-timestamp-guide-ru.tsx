export default function Content() {
  return (
    <div>
      <p>
        Откройте любой лог-файл. Посмотрите на поле <code>exp</code> в JWT-токене. Проверьте поле{" "}
        <code>created_at</code> в ответе API. Скорее всего, вы увидите число вроде{" "}
        <code>1711065600</code> или <code>1711065600000</code>. Это Unix-временная метка —
        простое целое число, представляющее момент времени. Понимание того, как работает Unix-время,
        откуда оно берётся и как избегать типичных ловушек, убережёт вас от целого класса багов —
        незаметных, трудновоспроизводимых и порой весьма неловких в продакшне.
      </p>
      <p>
        Вы можете мгновенно конвертировать любую Unix-временную метку в читаемую дату (и обратно)
        с помощью{" "}
        <a href="/tools/unix-timestamp">конвертера Unix-временных меток BrowseryTools</a> — бесплатно,
        без регистрации, всё остаётся в браузере.
      </p>

      <h2>Что такое Unix-временная метка?</h2>
      <p>
        Unix-временная метка — это количество секунд, прошедших с момента Unix Epoch: полуночи
        1 января 1970 года по всемирному координированному времени (UTC). Этот момент —
        00:00:00 UTC, 1970-01-01 — был выбран в качестве точки отсчёта при разработке операционной
        системы Unix в начале 1970-х годов. Это была недавняя и круглая дата, которая упрощала
        вычисления на аппаратном обеспечении той эпохи.
      </p>
      <p>
        Элегантность Unix-времени в том, что любой момент времени представлен одним целым числом.
        Сравнение двух временных меток — это вычитание. Проверка истечения срока — это сравнение.
        Добавление интервала — это сложение. Никаких часовых поясов, никакой календарной математики,
        никакого перехода на летнее время — только число.
      </p>
      <p>
        По состоянию на 2026 год текущая Unix-временная метка составляет примерно{" "}
        <code>1 774 000 000</code>. Каждую секунду это число увеличивается на 1.
      </p>

      <h2>Проблема 2038 года (Y2K38)</h2>
      <p>
        Если Unix-время хранится как 32-битное знаковое целое число — как это было во многих ранних
        реализациях — максимальное значение равно <code>2 147 483 647</code>. Это число соответствует
        03:14:07 UTC 19 января 2038 года. После этого момента 32-битное знаковое целое число
        переполняется и становится большим отрицательным числом, и системы, которые не были обновлены,
        будут неверно интерпретировать временные метки.
      </p>
      <p>
        Это проблема 2038 года (Y2K38) — Unix-эквивалент ошибки Y2K. Современные системы используют
        64-битные целые числа для временных меток, что расширяет представимый диапазон примерно до
        292 миллиардов лет в обоих направлениях — фактически вечность для любых практических целей.
        Однако встроенные системы, устаревшие базы данных с 32-битными столбцами временных меток и
        старый C-код, использующий <code>time_t</code> как 32-битный тип, всё ещё находятся в зоне риска.
      </p>

      <h2>Получение текущей временной метки</h2>
      <p>
        Вот как получить текущую Unix-временную метку на самых распространённых языках:
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// JavaScript — возвращает миллисекунды, делите на 1000 для секунд
const nowMs = Date.now();           // например, 1711065600000
const nowSec = Math.floor(Date.now() / 1000);  // например, 1711065600

// Python
import time
now = int(time.time())  # секунды с начала эпохи

# Через модуль datetime
from datetime import datetime, timezone
now = int(datetime.now(timezone.utc).timestamp())

// Go
import "time"
now := time.Now().Unix()         // секунды
nowNano := time.Now().UnixNano() // наносекунды

-- SQL (PostgreSQL)
SELECT EXTRACT(EPOCH FROM NOW())::BIGINT;

-- SQL (MySQL)
SELECT UNIX_TIMESTAMP();`}
      </pre>

      <h2>Преобразование временных меток в читаемые даты</h2>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// JavaScript — из секунд
const ts = 1711065600;
const date = new Date(ts * 1000);          // умножаем на 1000 для мс
console.log(date.toISOString());            // "2024-03-22T00:00:00.000Z"
console.log(date.toLocaleDateString());    // дата в локальном формате

// Python
import datetime
ts = 1711065600
dt = datetime.datetime.fromtimestamp(ts, tz=datetime.timezone.utc)
print(dt.isoformat())  # 2024-03-22T00:00:00+00:00

-- PostgreSQL: временная метка из целого числа
SELECT to_timestamp(1711065600);
-- Результат: 2024-03-22 00:00:00+00

-- MySQL
SELECT FROM_UNIXTIME(1711065600);
-- Результат: 2024-03-22 00:00:00`}
      </pre>

      <h2>Баг №1 с временными метками: миллисекунды против секунд</h2>
      <p>
        <code>Date.now()</code> в JavaScript возвращает миллисекунды. Unix-стандарт — и практически
        все остальные языки, базы данных и API — использует секунды. Это несоответствие является
        самым распространённым источником ошибок с временными метками.
      </p>
      <p>
        Симптомы характерны: даты отображаются как 1970 год (временная метка случайно делится на 1000
        или воспринимается как секунды, хотя на самом деле это миллисекунды), или даты попадают в
        56 000+ год (секунды воспринимаются как миллисекунды). Значение около{" "}
        <code>1 700 000 000</code> — почти наверняка секунды. Значение около{" "}
        <code>1 700 000 000 000</code> — почти наверняка миллисекунды.
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// Баг: секунды воспринимаются как миллисекунды — возвращает 1970 год
new Date(1711065600)        // Mon Jan 20 1970 11:24:25 UTC 🚫

// Правильно: умножаем секунды на 1000
new Date(1711065600 * 1000) // Fri Mar 22 2024 00:00:00 UTC ✓

// Защитная вспомогательная функция — работает и с секундами, и с миллисекундами
function toDate(ts) {
  // Если меньше 10^12 — это секунды; умножаем
  return new Date(ts < 1e12 ? ts * 1000 : ts);
}`}
      </pre>

      <h2>Проблемы с часовыми поясами</h2>
      <p>
        Unix-временные метки всегда в UTC — они представляют единственный абсолютный момент времени
        без привязки к часовому поясу. Вопрос часового пояса возникает только на уровне отображения,
        когда вы конвертируете временную метку в читаемый формат.
      </p>
      <p>
        Самая распространённая ошибка — использование методов локального часового пояса без осознания
        этого. <code>new Date(ts).toLocaleDateString()</code> в JavaScript возвращает дату в
        часовом поясе браузера. Если сервер генерирует метку в 23:00 UTC, а пользователи в UTC+0 и
        UTC+1 отображают её, они увидят разные календарные даты. Правильно это или нет — зависит от
        требований продукта, но это должен быть осознанный выбор, а не случайность.
      </p>
      <pre style={{background: "rgba(0,0,0,0.06)", borderRadius: "0.5rem", padding: "1rem 1.25rem", overflowX: "auto", fontFamily: "monospace", fontSize: "0.9rem", lineHeight: 1.7}}>
{`// Всегда явно указывайте часовой пояс — используйте toISOString() для UTC
const date = new Date(1711065600 * 1000);
date.toISOString()        // "2024-03-22T00:00:00.000Z"  ← всегда UTC

// Или используйте Intl.DateTimeFormat для отображения с локалью/часовым поясом
new Intl.DateTimeFormat("en-US", {
  timeZone: "America/New_York",
  dateStyle: "full",
}).format(date);  // "Friday, March 22, 2024"`}
      </pre>

      <h2>Временные метки в базах данных</h2>
      <p>
        Базы данных предлагают два основных варианта хранения дат: тип столбца <code>TIMESTAMP</code>{" "}
        (который хранит абсолютный момент времени) и тип <code>DATE</code> или <code>DATETIME</code>{" "}
        (который хранит календарное представление без часового пояса).
      </p>
      <p>
        Для полей вроде <code>created_at</code>, <code>updated_at</code> и временных меток событий
        всегда используйте столбец <code>TIMESTAMP WITH TIME ZONE</code> (или его эквивалент в вашей
        СУБД), а не просто целое число. Это позволяет базе данных корректно обрабатывать преобразование
        часовых поясов и сравнение, а запросы вроде «события за последние 24 часа» будут точными
        независимо от настроек часового пояса сервера.
      </p>
      <p>
        Если вам всё же нужно хранить Unix-временную метку как сырое целое число (для совместимости
        с внешними системами или максимальной переносимости), явно задокументируйте, секунды это
        или миллисекунды, и соблюдайте единообразие во всей схеме.
      </p>

      <h2>Временные метки в JWT и API</h2>
      <p>
        JSON Web Tokens (JWT) используют Unix-временные метки (в секундах) для своих временных claims:
      </p>
      <ul>
        <li><strong><code>iat</code></strong> — issued at («выдан в»): время создания токена</li>
        <li><strong><code>exp</code></strong> — expiry («срок действия»): время, после которого токен не должен приниматься</li>
        <li><strong><code>nbf</code></strong> — not before («не раньше»): токен не должен использоваться до этого времени</li>
      </ul>
      <p>
        Проверка истечения JWT — простое сравнение:{" "}
        <code>exp &gt; Math.floor(Date.now() / 1000)</code>. Если текущее время в секундах больше{" "}
        <code>exp</code>, токен истёк. Всегда валидируйте <code>exp</code> на сервере — никогда не
        полагайтесь только на клиентскую проверку срока действия.
      </p>

      <h2>Быстрый справочник: конвертация временных меток</h2>
      <p>
        Для быстрой и точной конвертации между Unix-временными метками и читаемыми датами используйте{" "}
        <a href="/tools/unix-timestamp">конвертер Unix-временных меток BrowseryTools</a>. Вставьте
        временную метку, чтобы увидеть соответствующую UTC-дату и локальную дату, или введите дату,
        чтобы получить её временную метку. Всё работает в браузере — никакого сервера, никакой слежки.
      </p>

      <h2>Итоги</h2>
      <p>
        Unix-временные метки — универсальный и однозначный способ представления моментов времени.
        Ключевые правила: они всегда в UTC, всегда в секундах (за исключением JavaScript, где{" "}
        <code>Date.now()</code> использует миллисекунды), и всегда положительное целое число для
        любой даты после 1970 года. Явно обрабатывайте различие миллисекунды/секунды, используйте
        UTC для хранения и передачи, и конвертируйте в локальное время только на уровне отображения.
      </p>
    </div>
  );
}
